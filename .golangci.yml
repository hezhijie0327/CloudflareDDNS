# CloudflareDDNS golangci-lint configuration.
# Run: golangci-lint run && golangci-lint fmt
version: "2"

linters:
  default: standard
  enable:
    # ── Correctness ──
    - bidichk
    - durationcheck
    - errname
    - errorlint
    - exhaustive
    - gosec
    - makezero
    - nilerr
    - nilnil
    - reassign
    - unconvert

    # ── Style / consistency ──
    - copyloopvar
    - decorder
    - dupword
    - gocritic
    - mirror
    - misspell
    - perfsprint
    - predeclared
    - usestdlibvars
    - usetesting
    - wastedassign

  disable:
    - gomodguard

  settings:
    decorder:
      dec-order:
        - type
        - const
        - var
        - func
      disable-dec-order-check: false
      disable-dec-num-check: true

    gocritic:
      enable-all: true

    exhaustive:
      default-signifies-exhaustive: true

    nilnil:
      checked-types: [ptr, map, chan]

    errname:
      error-prefix: Err
      error-type-suffix: Error

    usestdlibvars:
      time-month: true
      time-layout: true
      crypto-hash: true
      constant-kind: true

    errorlint:
      errorf: true
      errorf-multi: true
      asserts: true
      comparison: true

    perfsprint:
      int-conversion: true
      err-error: true
      errorf: true
      sprintf1: true

formatters:
  enable:
    - gofumpt
  settings:
    gofumpt:
      extra-rules: true
